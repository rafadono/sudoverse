{
  "name": "@sudoku/core",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "npx tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
